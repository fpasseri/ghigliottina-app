import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.passler.app',
  appName: 'La Ghigliottina',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
